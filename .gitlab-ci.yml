image: chilio/laravel-dusk-ci:stable

before_script:
  - php -v
  - composer install --prefer-dist --no-ansi --no-interaction --no-progress --no-scripts

cache:
  paths:
    - vendor
    - storage

#test:
#  stage: test
#  script:
#    - cp .env.example .env
#    - vendor/bin/phpunit
#  except:
#    - schedules

pages:
  stage: deploy
  script:
    - php arty packagist:providers
    - php arty packagist:get
    - cp -r storage/app/packagist/public/ public/
    - php arty packagist:index
  artifacts:
    paths:
      - public
    #  - storage
#  only:
#    - schedules
